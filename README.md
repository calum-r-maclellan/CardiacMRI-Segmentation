# CardiacMRI-Segmentation
PyTorch implementation of both vanilla segmentation networks (UNet and Attention UNet) and novel multi-task learning (MTL) UNet. The models are trained to segment the cardiac chambers (right ventricle, left ventricle) and surrounding myocardium tissue. 
